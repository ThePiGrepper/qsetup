#Qsetup is a group of dot files for my personal config
#It also comes with some additional scripts for easy installation

#[TODO]
- write an independent script for package installation.
- add an optional parameter for adding custom <pkg-list> file.
ie: $./home_setup -i <pkg-list>
- add more intelligence to installation script:
  - add new parsing capabilities to the script so it detects:
    - package groups //you can create custom groups of packages to partially install the contents of a pkg-list.
    ie: ./home_setup -i [<pkg-list>] --groups <group1> <group2> <group3>
- load different tmux status bar configs.
  - this would be useful for cli-only interfaces where all relevant system info must be enabled in the console.
    ie: network information(SSID,intensity),current volume level,usb devices detected.
- add 'IDE mode' for tmux.
  - enable shortcuts to move 'slide' panes to the sides. Style it so that it makes easier to work on applications on tmux.
- add clementine support on tmuxMusicBar.
	- if more than one music player are playing, display only the default one(Cmus).
	- to send actions to clementine on the CLI, just use clementine command.
	- to get status data(track playing,etc) use the following: qdbus-qt4
		qdbus-qt4 org.mpris.clementine /Player org.freedesktop.MediaPlayer.GetMetadata | grep title: |cut -d' ' -f2- //get current track title
	- Reference: https://github.com/clementine-player/Clementine/wiki/Controlling-Clementine-from-the-commandline-with-DBus-and-MPRIS
